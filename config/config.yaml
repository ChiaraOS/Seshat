mempalace:
  path: ./data/mempalace

ollama:
  base_url: http://localhost:11434
  model: llama3.1:8b

server:
  host: 0.0.0.0
  port: 8000
  # Wings to search when a question arrives with no explicit wing.
  # Leave empty to search ALL wings (recommended for multi-source setups).
  search_wings: []

logging:
  level: INFO
